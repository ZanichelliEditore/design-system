import {html} from "lit-html";
import {Meta, Story, Preview, Props} from "@storybook/addon-docs";
import {text, boolean, select} from "@storybook/addon-knobs";
import "./index";

<Meta
  title="Deprecated/ZStatusTag"
  component="z-status-tag"
/>

## This component is deprecated and will be removed from the library starting 09 August 2022, please use the z-tag component instead.

# ZStatusTag

<Preview>
  <Story name="ZStatusTag-sample">{html`<z-status-tag
    icon="${text("icon", "gear")}"
    text="${text("text", "in progress")}"
    expandable="${boolean("expandable", false)}"
    status="${select("status", ["warning", "error", "success", "default", "neutral"], "warning")}"
    variant="${select("variant", ["default-theme", "light", "dark"], "default-theme")}"
  ></z-status-tag>`}</Story>
</Preview>

<Props of="z-status-tag" />
