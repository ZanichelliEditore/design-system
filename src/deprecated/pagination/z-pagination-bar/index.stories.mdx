import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs";
import { number } from '@storybook/addon-knobs';
import "./index";

<Meta title="Deprecated/Pagination/ZPaginationBar" component="z-pagination-bar" />

# ZPaginationBar
## This component is deprecated and will be removed from the library starting 01 October 2022.

<Preview>
  <Story name="z-pagination-bar">
    {html`
      <z-pagination-bar
        pages="${number('pages', 20)}"
        visiblepages="${number('visiblepages', 5)}"
        currentpage="${number('currentpage', 4)}"
        startpage="${number('startpage', 2)}"
      ></z-pagination-bar>`}
  </Story>
</Preview>

<Props of="z-pagination-bar" />
