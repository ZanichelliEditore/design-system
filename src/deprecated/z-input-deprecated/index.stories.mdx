import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs";
import { text, boolean, number, radios, select } from "@storybook/addon-knobs";
import { InputStatusEnum } from "../../beans";
import "./index";

<Meta title="Deprecated/ZInputDeprecated" component="z-input-deprecated" />

## This component is deprecated and will be removed from the library starting October 1st 2022, please use z-input component instead.

# ZInputDeprecated

<Preview>
  <Story name="z-input-deprecated type:text">
    {html` <z-input-deprecated
      type="${select("type", ["text", "password", "number", "email"], "text")}"
      label="Label"
      placeholder="Placeholder text"
      disabled="${boolean("disabled", false)}"
      value="${text("value", "value")}"
      status="${select(
        "status",
        ["default", ...Object.keys(InputStatusEnum)],
        "default"
      )}"
      message="${text("message", "Helper text")}"
      readonly="${boolean("readonly", false)}"
    ></z-input-deprecated>`}
  </Story>
  <Story name="z-input-deprecated type:checkbox">
    {html`
      <z-input-deprecated
        type="checkbox"
        label="${text("label", "Checkbox Label")}"
        labelafter="${boolean("labelafter", true)}"
        disabled="${boolean("disabled", false)}"
        value="${text("value", "checkbox")}"
        checked="${boolean("checked", true)}"
      ></z-input-deprecated>
    `}
  </Story>
  <Story name="z-input-deprecated type:radio">
    {html`
      <z-input-deprecated
        type="radio"
        name="test-radio"
        id="radio1"
        label="${text("label", "Radio Label")}"
        labelafter="${boolean("labelafter", true)}"
        disabled="${boolean("disabled", false)}"
        value="${text("value", "radio")}"
        checked="${boolean("checked", false)}"
      ></z-input-deprecated>
    `}
  </Story>
  <Story name="z-input-deprecated type:select">
    {html` <z-input-deprecated
      type="select"
      label="${text("label", "Select Label")}"
      placeholder="Placeholder text"
      status="${text("status", "success")}"
      message="${text("message", "Helper text")}"
      value="${text("value", "select")}"
      items='[{"id":"item_1","name":"first item","selected":false},{"id":"item_2","name":"second item","selected":true}]'
    ></z-input-deprecated>`}
  </Story>
  <Story name="z-input-deprecated type:textarea">
    {html` <z-input-deprecated
      type="textarea"
      label="${text("label", "textarea")}"
      placeholder="Placeholder text"
      disabled="${boolean("disabled", false)}"
      value="${text(
        "value",
        "text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text text"
      )}"
      status="${select(
        "status",
        ["default", ...Object.keys(InputStatusEnum)],
        "default"
      )}"
      message="${text("message", "Helper text")}"
      readonly="${boolean("readonly", false)}"
    ></z-input-deprecated>`}
  </Story>
  <Story name="z-input-deprecated type:password">
    {html` <z-input-deprecated
      type="password"
      label="${text("label", "Password Label")}"
      placeholder="${text("placeholder", "Password")}"
    ></z-input-deprecated>`}
  </Story>
</Preview>

<Props of="z-input-deprecated" />
