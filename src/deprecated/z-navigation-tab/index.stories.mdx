import {html} from "lit-html";
import {Meta, Story, Canvas, ArgsTable} from "@storybook/addon-docs";
import {NavigationTabsSize, NavigationTabsOrientation} from "../../beans";
import {ICONS} from "../../components/icons/icons";
import "./index";

⚠️ This component is deprecated. Replace it with a native `<button>` element and use the `aria-controls` attribute to link it to the panel it controls.

# ZNavigationTab

<Meta
  title="Deprecated/ZNavigationTab"
  component="z-navigation-tab"
  argTypes={{
    selected: {
      control: "boolean",
    },
    disabled: {
      control: "boolean",
    },
    size: {
      control: {type: "inline-radio"},
      options: Object.values(NavigationTabsSize),
    },
    orientation: {
      control: {type: "inline-radio"},
      options: Object.values(NavigationTabsOrientation),
    },
    htmlTitle: {
      control: {type: "text"},
    },
    icon: {
      control: {type: "select"},
      options: Object.keys(ICONS).sort(),
    },
    label: {
      control: {type: "text"},
    },
    ariaControls: {
      control: {type: "text"},
    },
    tabId: {
      control: {type: "text"},
    },
  }}
/>

<Canvas>
  <Story
    name="z-navigation-tab"
    args={{
      selected: false,
      disabled: false,
      size: NavigationTabsSize.BIG,
      orientation: NavigationTabsOrientation.HORIZONTAL,
      htmlTitle: "Download stuff",
      icon: "download",
      label: "Download",
      ariaControls: "id-pannello",
      tabId: "label-pannello",
    }}
  >
    {(args) => html`
      <z-navigation-tab
        .selected=${args.selected}
        .disabled=${args.disabled}
        .size=${args.size}
        .orientation=${args.orientation}
        .htmlTitle=${args.htmlTitle}
        .icon=${args.icon}
        .label=${args.label}
        aria-controls=${args.ariaControls}
        tab-id=${args.tabId}
      ></z-navigation-tab>
    `}
  </Story>
</Canvas>

<ArgsTable of="z-navigation-tab" />
