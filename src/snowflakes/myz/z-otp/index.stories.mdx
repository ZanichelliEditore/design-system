import {html} from "lit-html";
import {Meta, Story, Preview, Props} from "@storybook/addon-docs";
import {text, boolean, select} from "@storybook/addon-knobs";
import "./index";

<Meta title="Myz/ZOtp" component="z-otp" />

# ZOtp

<Preview>
  <Story name="ZOtp-sample">{html`<z-otp
    input-num="${text("Inputs count", "6")}"
    status="${select("Input Status", ["", "error"], "")}"
    message="${text("Input message", "")}"
  ></z-otp>`}</Story>
</Preview>

<Props of="z-otp" />
