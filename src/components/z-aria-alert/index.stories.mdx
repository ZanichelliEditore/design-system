import {html} from "lit-html";
import {Meta, Story, Preview, Props} from "@storybook/addon-docs";
import {text, select} from "@storybook/addon-knobs";
import "./index";

<Meta
  title="Notification/ZAriaAlert"
  component="z-aria-alert"
/>

# ZAriaAlert

<Preview>
  <Story name="ZAriaAlert-sample">{html` <script>
      function myFunction() {
        const myTimeout = setTimeout(myDelayedFunction, 3000);
      }
      function myDelayedFunction() {
        var myalert = document.getElementById("idAlert");
        myalert.innerHTML = "Ecco un numero a caso: " + Math.random();
      }
    </script>
    <div>
      <h2 class="heading-2">ZAriaAlert base</h2>
      <z-body>
        Open your ScreenReader, Click on the button and wait 3 seconds, the screenreader will read a message
      </z-body>
      <button onclick="myFunction()">Click to say something</button>
      <z-aria-alert
        id="idAlert"
        mode="${select("mode", ["assertive", "polite"], "polite")}"
      >
        Testo NON nascosto
      </z-aria-alert>
    </div>`}</Story>
  <Story name="ZAriaAlert-VisuallyHidden">{html` <script>
      function myFunction2() {
        const myTimeout = setTimeout(myDelayedFunction2, 3000);
      }
      function myDelayedFunction2() {
        var myalert = document.getElementById("idAlert2");
        myalert.innerHTML = "Ecco un numero a caso: " + Math.random();
      }
    </script>
    <div>
      <h2 class="heading-2">ZAriaAlert with VisuallyHidden</h2>
      <z-body>
        You can also wrap your <code>z-aria-alert</code> with a <code>z-visually-hidden</code> tag in order to visually
        hide the text.
      </z-body>
      <z-body>
        Open your ScreenReader, Click on the button and wait 3 seconds, the screenreader will read a message
      </z-body>
      <button onclick="myFunction2()">Click to say something but visually hidden</button>
      <z-visually-hidden>
        <z-aria-alert
          id="idAlert2"
          mode="${select("mode", ["assertive", "polite"], "polite")}"
        >
          Testo qua
        </z-aria-alert>
      </z-visually-hidden>
    </div>`}</Story>
</Preview>

<Props of="z-aria-alert" />
