import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { text, select } from "@storybook/addon-knobs";

<Meta title="ZAriaAlert" component="z-aria-alert" />

# ZAriaAlert

<Preview>
  <Story name="ZAriaAlert-sample">
    {html`<z-aria-alert
      mode="${select(
          "mode",
          [
            "assertive",
            "polite"
          ],
          "polite"
        )}"
      text="${text("text", "Text aria-alert")}"
    </z-aria-alert>`}
  </Story>
</Preview>

<Props of="z-aria-alert" />
