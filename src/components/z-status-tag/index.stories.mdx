import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { text, boolean, select } from "@storybook/addon-knobs";

<Meta title="ZStatusTag" component="z-status-tag" />

# ZStatusTag

<Preview>
  <Story name="ZStatusTag-sample">{html`<z-status-tag
    icon="${text("icon", "gear")}"
    text="${text("text", "in progress")}"
    expandable="${boolean("expandable", false)}"
    status="${select(
      "status",
      ["warning", "error", "success", "default", "neutral"],
      "warning"
    )}"
    variant="${select(
      "variant",
      ["default-theme", "light", "dark"],
      "default-theme"
    )}"
  ></z-status-tag>`}</Story>
</Preview>

<Props of="z-status-tag" />
