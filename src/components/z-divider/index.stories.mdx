import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs";
import { text, select } from "@storybook/addon-knobs";
import "./index";

<Meta title="ZDivider" component="z-divider" />

# ZDivider

<Preview>
  <Story name="ZDivider">
    {html`<div style="height: 300px; padding: 24px">
      <z-divider
        size="${select("size", ["small", "medium", "large"], "small")}"
        color="${text("color token", "gray200")}"
        orientation="${select(
          "orientation",
          ["horizontal", "vertical"],
          "horizontal"
        )}"
      ></z-divider>
    </div>`}
  </Story>
</Preview>

<Props of="z-divider" />
