import { html } from "lit-html";
import { Meta, Canvas, Story, Preview, Props } from "@storybook/addon-docs";
import { text, boolean } from "@storybook/addon-knobs";

import "./index";

<Meta
  title="ZTag"
  component="z-tag"
  argTypes={{
    icon: { control: { type: "text" } },
    text: { control: { type: "text" } },
    expandable: { control: { type: "boolean" } },
  }}
/>

# ZTag

<Canvas>
  <Story
    name="ZTag-sample"
    args={{
      icon: "gear",
      text: "In progress",
      expandable: false,
    }}
  >
    {(args) => html`<z-tag .icon=${args.icon} .expandable=${args.expandable}
      >${args.text}
    </z-tag>`}
  </Story>
</Canvas>

# ZTag token

<Canvas>
  <Story
    name="ZTag-token"
    args={{
      icon: "gear",
      text: "In progress",
      expandable: false,
      iconTextToken: "--color-white",
      bgToken: "--avatar-C19",
    }}
  >
    {(args) => html`<z-tag
      style="--iconTextToken: var(${args.token}); --bgToken: var(${args.bgToken})"
      .icon=${args.icon}
      .expandable=${args.expandable}
      >${args.text}
    </z-tag>`}
  </Story>
</Canvas>

# ZTag token

<Canvas>
  <Story
    name="ZTag-with-long-text"
    args={{
      icon: "gear",
      text: "In progress testo lungo molto lungo lungo lungo lungo lungolungo lungolungo",
      expandable: false,
      iconTextToken: "--color-white",
      bgToken: "--avatar-C19",
    }}
  >
    {(args) => html`<z-tag
      style="--iconTextToken: var(${args.token}); --bgToken: var(${args.bgToken}); max-width: 500px;"
      .icon=${args.icon}
      .expandable=${args.expandable}
      >${args.text}
    </z-tag>`}
  </Story>
</Canvas>

<Props of="z-tag" />
