import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs";
import { text, radios, boolean } from "@storybook/addon-knobs";
import { PopoverPosition } from "../../beans";
import "./index.stories.css";
import "./index";

## Stories

<Meta title="ZPopover" component="z-popover" />

# ZPopover

<Preview>
  <Story name="contextual-menu-like">
    {(args) => html`
      <div class="popover-container">
        <z-popover
          position="${radios("type", PopoverPosition, "right_bottom")}"
          open="${boolean("open", true)}"
          center="${boolean("center", false)}"
          show-arrow="${boolean("show-arrow", false)}"
          bind-to="#icon"
        >
          <div class="container">
            <z-button-deprecated>Open</z-button-deprecated>
            <z-button-deprecated>Save as</z-button-deprecated>
            <z-button-deprecated>Share</z-button-deprecated>
          </div>
        </z-popover>
        <z-icon id="icon" name="plus-square-filled"></z-icon>
      </div>
    `}
  </Story>
</Preview>

# ZPopover as ZTooltip

<Preview>
  <Story name="tooltip">
    {(args) => html`
      <div class="popover-container">
        <z-popover
          position="${radios("type", PopoverPosition, "top")}"
          open=${true}
          center=${true}
          show-arrow=${true}
          bind-to="#icon"
        >
          <div class="container">
            <z-button-deprecated>Open</z-button-deprecated>
            <z-button-deprecated>Save as</z-button-deprecated>
            <z-button-deprecated>Share</z-button-deprecated>
          </div>
        </z-popover>
        <z-icon id="icon" name="plus-square-filled"></z-icon>
      </div>
    `}
  </Story>
</Preview>

<Props of="z-popover" />
