import { html } from "lit-html";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { PopoverPosition, PopoverShadow } from "../../beans";

import "./index";

<Meta
  title="ZPopover"
  component="z-popover"
  argTypes={{
    position: {
      control: { type: "select" },
      options: Object.values(PopoverPosition),
    },
    "show-arrow": { control: { type: "boolean" } },
    "background-color": { control: { type: "text" } },
    "box-shadow": {
      control: { type: "select" },
      options: Object.values(PopoverShadow),
    },
    "border-radius": {
      control: { type: "select" },
      options: ["none", "small", "medium"],
    },
}}
/>

# ZPopover

<Canvas>
  <Story
    name="ZPopover-sample"
    args={{
      position: "after-down",
      "show-arrow": false,
      "background-color": "color-white",
      "box-shadow": "shadow-1",
      "border-radius": "small",
    }}
  >
    {(args) => html`<div
      style="height: 200px; display: flex; align-items: center; justify-content: center"
    >
      <z-popover
        position=${args.position}
        show-arrow=${args["show-arrow"]}
        background-color=${args["background-color"]}
        box-shadow=${args["box-shadow"]}
        border-radius=${args["border-radius"]}
      >
        <z-button slot="trigger" icon="gear">Impostazioni</z-button>
        <div slot="popover">Contenuto Popover</div>
      </z-popover>
    </div>`}
  </Story>
</Canvas>

<ArgsTable of="z-popover" />
