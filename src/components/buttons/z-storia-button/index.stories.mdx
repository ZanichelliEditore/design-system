import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { select } from '@storybook/addon-knobs';

<Meta title="Snowflake/Storia" component="z-storia-button" />

# ZStoriaButton

<Preview>
  <Story name="ZStoriaButton-button">
    {(args) => html`<z-storia-button variant="${select('Variant', ['primary', 'secondary', 'tertiary'], 'primary')}">
        <button>Invio</button>
      </z-storia-button>`}
  </Story>
  <Story name="ZStoriaButton-link">
    {(args) => html`<z-storia-button variant="${select('Variant', ['primary', 'secondary', 'tertiary'], 'primary')}">
        <a href="https://wikipedia.com" target="_blank">Wikipedia</a>
      </z-storia-button>`}
  </Story>
</Preview>
