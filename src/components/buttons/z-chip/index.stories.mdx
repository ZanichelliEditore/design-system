import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs";
import { text, select, boolean } from "@storybook/addon-knobs";
import "./index";

## Stories

<Meta title="Buttons/ZChip" component="z-chip" />

# ZChip

<Preview>
  <Story name="z-chip">
    {html` <z-chip
      type="${select("type", ["default", "medium", "small"], "default")}"
      icon="${text("Icon name", "pdf")}"
    >
      ${text("text", "my custom element")}
    </z-chip>`}
  </Story>
</Preview>

# ZChip with interactiveIcon

<Preview>
  <Story name="z-chip with interactiveIcon">
    {html` <z-chip
      type="${select("type", ["default", "medium", "small"], "default")}"
      interactive-icon="${text("Interactive icon", "multiply-circled")}"
      icon="${text("Icon name", "pdf")}"
      disabled="${boolean("disabled", false)}"
    >
      ${text("text", "my custom element")}
    </z-chip>`}
  </Story>
</Preview>

<Props of="z-chip" />
