import {html} from "lit-html";
import {Meta, Canvas, Story, ArgsTable} from "@storybook/addon-docs";
import {ZChipType} from "../../../beans";
import "./index";

<Meta
  title="Buttons/ZChip"
  component="z-chip"
  argTypes={{
    type: {
      options: Object.values(ZChipType),
      control: {type: "radio"},
    },
  }}
/>

# Zchip

<Canvas>
  <Story
    name="z-chip"
    args={{
      type: ZChipType.DEFAULT,
      icon: "pdf",
    }}
  >
    {(args) => html` <z-chip type=${args.type} icon=${args.icon}>my custom element</z-chip> `}
  </Story>
</Canvas>

# Zchip different font-weight

<Canvas>
  <Story
    name="z-chip different font-weight"
    args={{
      type: ZChipType.DEFAULT,
      icon: "pdf",
    }}
  >
    {(args) =>
      html`
        <z-chip type=${args.type} icon=${args.icon}>
          <strong>my</strong>
          <span style="margin-left: 4px"> custom element</span>
        </z-chip>
      `
    }
  </Story>
</Canvas>

# Zchip interactive icon

<Canvas>
  <Story
    name="z-chip interactive icon"
    args={{
      type: ZChipType.DEFAULT,
      icon: "pdf",
      interactiveIcon: "multiply-circled",
      disabled: false,
    }}
  >
    {(args) =>
      html` <z-chip
        type=${args.type}
        icon=${args.icon}
        disabled=${args.disabled}
        interactive-icon=${args.interactiveIcon}
      >
        my custom element
      </z-chip>`
    }
  </Story>
</Canvas>

<ArgsTable of="z-chip" />
