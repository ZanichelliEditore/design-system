import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { text, select, boolean } from "@storybook/addon-knobs";

## Stories

<Meta title="Buttons/ZChip" component="z-chip" />

# ZChip with attributes

<Preview>
  <Story name="z-chip">
    {html` <z-chip
      type="${select("type", ["default", "medium", "small"], "default")}"
      icon-position="${select("Icon position", ["left", "right"], "left")}"
      label="${text("label", "label")}"
      icon="${text("Icon name", "pdf")}"
      bold="${boolean("bold", false)}"
      disabled="${boolean("disabled", false)}"
      interaction="${boolean("Interaction", false)}"
    >
      ${text("text", "my custom element")}
    </z-chip>`}
  </Story>
</Preview>

<Props of="z-chip" />
