import {html} from "lit-html";
import {Meta, Story, Canvas, ArgsTable} from "@storybook/addon-docs";
import {text, boolean} from "@storybook/addon-knobs";
import "./index";

## Stories

<Meta
  title="Buttons/ZToggleButton"
  component="z-toggle-button"
  argTypes={{
    label: {
      control: {type: "text"},
    },
    avoidClick: {
      control: {type: "boolean"},
    },
    isdisabled: {
      control: {type: "boolean"},
    },
    opened: {
      control: {type: "boolean"},
    },
  }}
/>

# ZToggleButton

<Canvas>
  <Story
    name="z-toggle-button"
    args={{
      label: "Toggle",
      avoidClick: false,
      isdisabled: false,
      opened: false,
    }}
  >
    {(args) => html`
      <z-toggle-button
        label=${args.label}
        avoidClick=${args.avoidClick}
        isdisabled=${args.isdisabled}
        opened=${args.opened}
      ></z-toggle-button>
    `}
  </Story>
</Canvas>

<ArgsTable of="z-toggle-button" />
