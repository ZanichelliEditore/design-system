import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs";
import { text, boolean, number } from '@storybook/addon-knobs';

## Stories

<Meta title="Buttons/ZButtonSort" component="z-button-sort" />

  # ZButtonSort

<Preview>
  <Story name="z-button-sort">
    {html`
      <z-button-sort
        label="${text('label', 'sort')}"
        counter="${number('counter', 3)}"
        desclabel="${text('desclabel', 'label DESC')}"
        sortlabelasc="${text('sortlabelasc', '1-9')}"
        sortlabeldesc="${text('sortlabeldesc', '9-1')}"
        isselected="${boolean('isselected', true)}"
        sortasc="${boolean('sortasc', true)}"
      ></z-button-sort>`}
  </Story>
  <Story name="z-button-sort ellipsis">
    {html`
      <z-button-sort
        label="${text('label', 'Testo molto lungo lungo lungo lunghissimo lunghissimo lunghissimo ancora di più')}"
        counter="${number('counter', 3)}"
        desclabel="${text('desclabel', 'label DESC')}"
        sortlabelasc="${text('sortlabelasc', '1-9')}"
        sortlabeldesc="${text('sortlabeldesc', '9-1')}"
        isselected="${boolean('isselected', true)}"
        sortasc="${boolean('sortasc', true)}"
      ></z-button-sort>`}
  </Story>
</Preview>

  <Props of="z-button-sort" />

