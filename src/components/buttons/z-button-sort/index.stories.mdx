import {html} from "lit-html";
import {Meta, Story, Canvas, ArgsTable} from "@storybook/addon-docs";
import "./index";

<Meta
  title="Buttons/ZButtonSort"
  component="z-button-sort"
  args={{
    label: "sort",
    counter: 3,
    desclabel: "label DESC",
    sortlabelasc: "1-9",
    sortlabeldesc: "9-1",
    isselected: true,
    sortasc: true,
  }}
/>

# ZButtonSort

<Canvas>
  <Story name="z-button-sort">
    {(args) => html`<z-button-sort
      label="${args.label}"
      counter="${args.counter}"
      desclabel="${args.desclabel}"
      sortlabelasc="${args.sortlabelasc}"
      sortlabeldesc="${args.sortlabeldesc}"
      isselected="${args.isselected}"
      sortasc="${args.sortasc}"
    ></z-button-sort>`}
  </Story>
</Canvas>

## ZButtonSort ellipsis

<Canvas>
  <Story
    name="z-button-sort ellipsis"
    args={{
      label: "Testo molto lungo lungo lungo lunghissimo lunghissimo lunghissimo ancora di più",
    }}
  >
    {(args) => html`<z-button-sort
      label="${args.label}"
      counter="${args.counter}"
      desclabel="${args.desclabel}"
      sortlabelasc="${args.sortlabelasc}"
      sortlabeldesc="${args.sortlabeldesc}"
      isselected="${args.isselected}"
      sortasc="${args.sortasc}"
    ></z-button-sort>`}
  </Story>
</Canvas>

<ArgsTable of="z-button-sort" />
