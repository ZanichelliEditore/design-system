import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { text, boolean, select } from '@storybook/addon-knobs';

## Stories

<Meta title="Buttons/ZButton" component="z-button" />

# ZButton

<Preview>
  <Story name="button">
    {(args) => html`
      <z-button
        variant="${select('Variant', ['primary', 'secondary', 'tertiary'], 'primary')}"
        .issmall="${boolean('issmall', false)}"
      >
        <button slot="element" .disabled="${boolean('disabled', false)}">
          <z-icon width="16" height="16" name="${text('icon', 'download')}"></z-icon>Enter
        </button>
      </z-button>`}
  </Story>
</Preview>

# ZButton with Link

<Preview>
  <Story name="link">
    {(args) => html`
      <z-button
        variant="${select('Variant', ['primary', 'secondary', 'tertiary'], 'primary')}"
        .issmall="${boolean('issmall', false)}"
      >
        <a slot="element" href="https://wikipedia.com" target="_blank">
          <z-icon width="16" height="16" name="${text('icon', 'download')}"></z-icon>Go to Wikipedia
        </a>
      </z-button>`}
  </Story>
</Preview>

<Preview>
  <Story name="legacy">
    {(args) => html`
      <z-button
        variant="${select('Variant', ['primary', 'secondary', 'tertiary'], 'primary')}"
        disabled="${boolean('disabled', false)}"
        icon="${text('icon', 'download')}"
        .issmall="${boolean('issmall', false)}"
      >${text('label', 'click me')}</z-button>`}
  </Story>
</Preview>

<Props of="z-button" />
