import { html } from "lit-html";
import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import {
  ButtonVariantEnum,
  ButtonTypeEnum,
  ButtonSizeEnum,
} from "../../../beans";
import "./index";

<Meta
  title="Buttons/ZButton"
  component="z-button"
  argTypes={{
    variant: {
      options: Object.values(ButtonVariantEnum),
      control: { type: "radio" },
    },
    size: {
      options: Object.values(ButtonSizeEnum),
      control: { type: "radio" },
    },
  }}
/>

# ZButton

## Default

<Canvas>
  <Story
    name="Default button"
    args={{
      variant: ButtonVariantEnum.primary,
      size: ButtonSizeEnum.big,
      disabled: false,
      icon: "download",
      text: "Click me",
    }}
  >
    {(args) => html`
      <div style="width: 200px">
        <z-button
          variant=${args.variant}
          size=${args.size}
          disabled=${args.disabled}
          icon=${args.icon}
          text=${args.text}
        ></z-button>
      </div>
    `}
  </Story>
</Canvas>

## Tertiary small

<Canvas>
  <Story
    name="Tertiary small"
    args={{
      variant: ButtonVariantEnum["tertiary"],
      size: ButtonSizeEnum.small,
      disabled: false,
      icon: "download",
      text: "button",
    }}
    parameters={{
      controls: {
        exclude: ["variant", "icon"],
      },
    }}
  >
    {(args) => html`
      <z-button
        variant=${args.variant}
        size=${args.size}
        disabled=${args.disabled}
        icon=${args.icon}
        text=${args.text}
      >
      </z-button>
    `}
  </Story>
</Canvas>

## Secondary x-small

<Canvas>
  <Story
    name="secondary x-small"
    args={{
      variant: ButtonVariantEnum["secondary"],
      size: ButtonSizeEnum["x-small"],
      disabled: false,
      icon: "download",
      text: "button",
    }}
    parameters={{
      controls: {
        exclude: ["variant", "icon"],
      },
    }}
  >
    {(args) => html`
      <z-button
        variant=${args.variant}
        size=${args.size}
        disabled=${args.disabled}
        icon=${args.icon}
        text=${args.text}
      >
      </z-button>
    `}
  </Story>
</Canvas>

## Icon only

It is possible use a button with icon only passing an icon name to `icon` prop, without set `text` prop.
**Note:** `z-icon` drops its spacing accordingly.

| Prop name | Prop value        |
| --------- | ----------------- |
| icon      | Any `z-icon` name |

<Canvas>
  <Story
    name="Icon only"
    args={{
      variant: ButtonVariantEnum.primary,
      size: ButtonSizeEnum.big,
      disabled: false,
      icon: "download",
    }}
    parameters={{
      controls: {
        exclude: ["square"],
      },
    }}
  >
    {(args) => html`
      <z-button
        variant="${args.variant}"
        size="${args.size}"
        disabled="${args.disabled}"
        icon="${args.icon}"
      ></z-button>
    `}
  </Story>
</Canvas>

## Zbutton as a link

It is possible for z-button to be an html `a` element.

To do this you need to set `href` prop.

<Canvas>
  <Story
    name="With href prop"
    args={{
      variant: ButtonVariantEnum.primary,
      size: ButtonSizeEnum.big,
      disabled: false,
      icon: "arrow-quad-north-east",
      href: "https://wikipedia.com",
      target: "_blank",
      text: "Go to Wikipedia",
    }}
  >
    {(args) => html`
      <z-button
        icon="${args.icon}"
        href="${args.href}"
        target="${args.target}"
        variant="${args.variant}"
        size="${args.size}"
        disabled="${args.disabled}"
        text="${args.text}"
      >
      </z-button>
    `}
  </Story>
</Canvas>

Without `href` prop (it will be a button).

<Canvas>
  <Story
    name="Without href prop (it will be a button)"
    args={{
      variant: ButtonVariantEnum.primary,
      size: ButtonSizeEnum.big,
      disabled: false,
      icon: "link",
    }}
  >
    {(args) => html`
      <z-button
        icon="${args.icon}"
        variant="${args.variant}"
        size="${args.size}"
        disabled="${args.disabled}"
      >
      </z-button>
    `}
  </Story>
</Canvas>

<ArgsTable of="z-button" />
