import { html } from "lit-html";
import { Meta, Canvas, Story, Preview, Props } from "@storybook/addon-docs";
import { LabelPositionEnum } from "../../../beans";
import "./index";

<Meta
  title="Buttons/ZToggleSwitch"
  component="z-toggle-switch"
  argTypes={{
    labelPosition: {
      options: Object.values(LabelPositionEnum),
      control: { type: "radio" },
    },
  }}
/>

# ZToggleSwitch

<Canvas>
  <Story
    name="z-toggle-switch"
    args={{
      labelPosition: LabelPositionEnum.left,
      disabled: false,
    }}
  >
    {(args) =>
      html`
        <z-toggle-switch
          label-position=${args.labelPosition}
          disabled=${args.disabled}
          >Toggle
        </z-toggle-switch>
      `
    }
  </Story>
</Canvas>

<ArgsTable of="z-toggle-switch" />
