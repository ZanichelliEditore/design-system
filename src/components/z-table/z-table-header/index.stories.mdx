import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs";
import { boolean, select } from "@storybook/addon-knobs";
import "./index";

<Meta
  title="Table/ZTableHeader"
  component="z-table-header"
/>

# ZTableHeader

<Preview>
  <Story name="ZTableHeader-sample">
    {html`<z-table bordered="true">
      <z-table-head>
        <z-table-row>
          <z-table-header
            column-id="col-1"
            sortable="${boolean("sortable", false)}"
            show-button="${boolean("showButton", false)}"
            padding="${select(
              "padding",
              ["x-small", "small", "medium", "large", "special"],
              "medium"
            )}"
          >
            <span>Titolo 1</span>
          </z-table-header>
          <z-table-header
            column-id="col-2"
            sortable="false"
            show-button="${boolean("showButton", false)}"
            padding="${select(
              "padding",
              ["x-small", "small", "medium", "large", "special"],
              "medium"
            )}"
          >
            <span>Titolo 2</span>
          </z-table-header>
          <z-table-header
            column-id="col-3"
            sortable="${boolean("sortable", false)}"
            show-button="${boolean("showButton", false)}"
            padding="${select(
              "padding",
              ["x-small", "small", "medium", "large", "special"],
              "medium"
            )}"
          >
            <span>Titolo 3</span>
          </z-table-header>
          <z-table-header
            column-id="col-4"
            sortable="${boolean("sortable", false)}"
            show-button="${boolean("showButton", false)}"
            padding="${select(
              "padding",
              ["x-small", "small", "medium", "large", "special"],
              "medium"
            )}"
          >
            <span>Titolo 4</span>
          </z-table-header>
        </z-table-row>
      </z-table-head>
      <z-table-body>
        <z-table-row>
          <z-table-cell>Valore</z-table-cell>
          <z-table-cell>Valore</z-table-cell>
          <z-table-cell>Valore</z-table-cell>
          <z-table-cell>Valore</z-table-cell>
        </z-table-row>
      </z-table-body>
    </z-table>`}
  </Story>
</Preview>

<Props of="z-table-header" />

## Callback for sorting

Pass **onSort** prop as a function that accepts an object structured like this:

> { sortingOrientation: "asc" }

## Note

**columnId** prop is mandatory for sortable header otherwise there will be an error.
