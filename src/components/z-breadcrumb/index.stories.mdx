import {Meta, Canvas, Story, ArgsTable} from "@storybook/addon-docs";
import {BreadcrumbPathType} from "../../beans";
import {html} from "lit-html";
import "./index";

<Meta
  title="ZBreadcrumb"
  component="z-breadcrumb"
  argTypes={{
    paths: {
      control: {type: "object"},
    },
    type: {
      options: Object.values(BreadcrumbPathType),
      control: {type: "select"},
    },
  }}
/>

# ZBreadcrumb

<Canvas>
  <Story
    name="breadcrumb with paths"
    args={{
      type: "semibold",
      paths: [
        {name: "Path 1", path: "/path1"},
        {name: "Path 2", path: "/path2"},
        {name: "Path 3", path: "/path3"},
      ],
    }}
  >
    {(args) => html`<z-breadcrumb
      type=${args.type}
      paths=${JSON.stringify(args.paths)}
    ></z-breadcrumb>`}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="breadcrumb with slot"
    args={{
      type: "semibold",
    }}
  >
    {(args) =>
      html`<z-breadcrumb type=${args.type}
        ><div slot="breadcrumb-content">
          <z-link
            href="/path1"
            textcolor="primary"
            iconposition="left"
            htmlid="1"
            htmltabindex="1"
            icon="chevron-left"
            >Path 1</z-link
          >
        </div></z-breadcrumb
      >`
    }
  </Story>
</Canvas>

<ArgsTable of="z-breadcrumb" />
