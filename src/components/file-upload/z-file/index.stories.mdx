import {html} from "lit-html";
import {Meta, Story, Canvas, ArgsTable} from "@storybook/addon-docs";

import "./index";

<Meta title="Buttons/ZFile" component="z-file" />

# ZFile

<Canvas>
  <Story
    name="ZFile"
    args={{
      fileNumber: 1,
      fileName: "abcdefghilmnopqrst.jpg",
    }}
  >
    {(args) => html`<z-file file-number="${args.fileNumber}" file-name="${args.fileName}"></z-file>`}
  </Story>
</Canvas>

<ArgsTable of="z-file" />
