import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { text } from "@storybook/addon-knobs";

<Meta title="ContextualMenu" component="contextual-menu" />

# ContextualMenu

<Preview>
  <Story name="ContextualMenu with icons">
    {html` <div
      style="height: 200px; display: flex; align-items: center; justify-content: center"
    >
      <contextual-menu
        id="contextual-menu-1"
        elements='[{"icon": "share", "text": "Element 1"}, {"icon": "delete", "text": "Elemento 2"}]'
        color="${text("color", "color-primary01")}"
      >
      </contextual-menu>
    </div>`}
  </Story>
</Preview>

<Preview>
  <Story name="ContextualMenu without icons">
    {html` <div
      style="height: 200px; display: flex; align-items: center; justify-content: center"
    >
      <contextual-menu
        id="contextual-menu-2"
        elements='[{ "text": "Element 1"}, { "text": "Elemento 2"}, { "text": "Elemento 3"}]'
        color="${text("color", "color-primary01")}"
      >
      </contextual-menu>
    </div>`}
  </Story>
</Preview>

<Props of="contextual-menu" />


## How to use

### elements prop
This component receives a prop called "elements" which consists of an array of objects with two attributes:
- icon (optional): the icon of the list element;
- text: the text of the list element;
In order to pass this property correctly, the array must be converted into a string

#### N.B.
The icons are shown only if all the elements have valued this attribute

### onClickContextualMenu event
This component also have an Event that manages the behaviour of the contextual-menu items.
The callback function receives the index of the clicked item and must handle all the contextual-menu items
using - for example - a switch statement.
