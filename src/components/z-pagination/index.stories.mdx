import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-html";
import "./index";

<Meta title="ZPagination" component="z-pagination" argTypes={{
  navArrows: { control: { type: "boolean" } },
  totalPages: { control: { type: "number", min: 1 } },
  visiblePages: { control: { type: "number", min: 1 } },
  skip: { control: { type: "number", min: 1 } },
  edges: { control: { type: "boolean" } },
  goToPage: { control: { type: "boolean" } },
  split: { control: { type: "number", min: 1 } },
}} />

# ZPagination - visible pages

<Canvas>
  <Story
    name="ZPagination - visible pages"
    args={{
      navArrows: true,
      totalPages: 201,
      visiblePages: 5,
      goToPage: false,
    }}
    parameters={{
      controls: {
        exclude: ["skip", "edges", "split"],
      }
    }}
  >
    {(args) => html`
      <z-pagination
        .navArrows=${args.navArrows}
        .totalPages=${args.totalPages}
        .visiblePages=${args.visiblePages}
        .goToPage=${args.goToPage}
      ></z-pagination>
    `}
  </Story>
</Canvas>

# ZPagination - split

> Note that `visiblePages` and `split` are mutually exclusive.

<Canvas>
  <Story
    name="ZPagination - split"
    args={{
      navArrows: true,
      totalPages: 201,
      split: 1,
      goToPage: false,
    }}
    parameters={{
      controls: {
        exclude: ["skip", "edges", "visiblePages"],
      }
    }}
  >
    {(args) => html`
      <z-pagination
        .navArrows=${args.navArrows}
        .totalPages=${args.totalPages}
        .split=${args.split}
        .goToPage=${args.goToPage}
      ></z-pagination>
    `}
  </Story>
</Canvas>

# ZPagination - edges

<Canvas>
  <Story
    name="ZPagination - edges"
    args={{
      navArrows: true,
      totalPages: 201,
      visiblePages: 5,
      edges: true,
      goToPage: false,
    }}
    parameters={{
      controls: {
        exclude: ["skip", "split"],
      }
    }}
  >
    {(args) => html`
      <z-pagination
        .navArrows=${args.navArrows}
        .totalPages=${args.totalPages}
        .visiblePages=${args.visiblePages}
        .edges=${args.edges}
        .goToPage=${args.goToPage}
      ></z-pagination>
    `}
  </Story>
</Canvas>

# ZPagination - skip

<Canvas>
  <Story
    name="ZPagination - skip"
    args={{
      navArrows: true,
      totalPages: 201,
      visiblePages: 5,
      skip: 10,
      goToPage: false,
    }}
    parameters={{
      controls: {
        exclude: ["edges", "split"],
      }
    }}
  >
    {(args) => html`
      <z-pagination
        .navArrows=${args.navArrows}
        .totalPages=${args.totalPages}
        .visiblePages=${args.visiblePages}
        .skip=${args.skip}
        .goToPage=${args.goToPage}
      ></z-pagination>
    `}
  </Story>
</Canvas>

<ArgsTable of="z-pagination" />
