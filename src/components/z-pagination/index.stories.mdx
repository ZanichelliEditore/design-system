import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-html";
import "./index";

<Meta title="ZPagination" component="z-pagination" argTypes={{
  navArrows: { control: { type: "boolean" } },
  totalPages: { control: { type: "number", min: 1 } },
  visiblePages: { control: { type: "number", min: 1 }, if: { arg: "split", truthy: false } },
  skip: { control: { type: "number", min: 1 } },
  edges: { control: { type: "boolean" }, if: { arg: "split", truthy: false } },
  goToPage: { control: { type: "boolean" } },
  split: { control: { type: "number", min: 1 } },
}} />

# ZPagination - visible pages

<Canvas>
  <Story
    name="ZPagination - visible pages"
    args={{
      navArrows: true,
      totalPages: 201,
      visiblePages: 5,
      goToPage: false,
      split: false,
      skip: false,
      edges: false,
    }}
    parameters={{
      controls: {
        exclude: ["split"],
      }
    }}
  >
    {(args) => html`
      <z-pagination
        .navArrows=${args.navArrows}
        .totalPages=${args.totalPages}
        .visiblePages=${args.visiblePages}
        .skip=${args.skip}
        .goToPage=${args.goToPage}
        .edges=${args.edges}
      ></z-pagination>
    `}
  </Story>
</Canvas>

# ZPagination - split

> Note that enabling `split` will shut off `visiblePages` and `edges`.

<Canvas>
  <Story
    name="ZPagination - split"
    args={{
      navArrows: true,
      totalPages: 201,
      split: 1,
      skip: false,
      goToPage: false,
    }}
    parameters={{
      controls: {
        exclude: ["edges", "visiblePages"],
      }
    }}
  >
    {(args) => html`
      <z-pagination
        .navArrows=${args.navArrows}
        .totalPages=${args.totalPages}
        .split=${args.split}
        .skip=${args.skip}
        .goToPage=${args.goToPage}
      ></z-pagination>
    `}
  </Story>
</Canvas>

# ZPagination - edges

<Canvas>
  <Story
    name="ZPagination - edges"
    args={{
      navArrows: true,
      totalPages: 201,
      visiblePages: 5,
      edges: true,
      goToPage: false,
      split: false,
    }}
    parameters={{
      controls: {
        exclude: ["skip", "split"],
      }
    }}
  >
    {(args) => html`
      <z-pagination
        .navArrows=${args.navArrows}
        .totalPages=${args.totalPages}
        .visiblePages=${args.visiblePages}
        .edges=${args.edges}
        .goToPage=${args.goToPage}
      ></z-pagination>
    `}
  </Story>
</Canvas>

# ZPagination - skip

<Canvas>
  <Story
    name="ZPagination - skip"
    args={{
      navArrows: true,
      totalPages: 201,
      visiblePages: 5,
      skip: 10,
      goToPage: false,
      edges: false,
      split: false,
    }}
  >
    {(args) => html`
      <z-pagination
        .navArrows=${args.navArrows}
        .totalPages=${args.totalPages}
        .visiblePages=${args.visiblePages}
        .skip=${args.skip}
        .goToPage=${args.goToPage}
        .edges=${args.edges}
        .split=${args.split}
      ></z-pagination>
    `}
  </Story>
</Canvas>

<ArgsTable of="z-pagination" />
