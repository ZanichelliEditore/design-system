import {html} from "lit-html";
import {Meta, Story, Preview, Props} from "@storybook/addon-docs";
import {text, number, boolean} from "@storybook/addon-knobs";
import "./index";

## Stories

<Meta title="Logo/ZLogo" component="z-logo" />

# ZLogo

<Preview>
  <Story name="z-logo">
    {html` <z-logo
      image-alt="${text("image alt", "logo")}"
      height="${number("height", 50)}"
      width="${number("width", 200)}"
      link="${text("link", "")}"
      target-blank=${boolean("target blank", false)}
      mobile-logo=${boolean("mobile logo", false)}
    ></z-logo>`}
  </Story>
</Preview>

<Props of="z-logo" />
