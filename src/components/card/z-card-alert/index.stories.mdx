import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { text } from '@storybook/addon-knobs';

## Stories

<Meta title="Card/ZCardAlert" component="z-card-alert" />

  # ZCardAlert

<Preview>
  <Story name="z-card-alert">
    {html`
      <z-card-alert
        actiontext="${text('actiontext', 'Annulla')}"
        contenttext="${text('contenttext', 'Libro aggiunto ai miei libri preferiti')}"
        iconName="${text('iconName', 'circle-check')}"
        type="${text('type', 'add')}"
      ></z-card-alert>`}
  </Story>
</Preview>

  <Props of="z-card-alert" />

