import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { text, boolean } from '@storybook/addon-knobs';

## Stories

<Meta title="Card/ZCardFooter" component="z-card-footer" />

  # ZCardFooter

<Preview>
  <Story name="z-card-footer">
    {html`
      <z-card-footer
        autori="${text('autori', 'Authors')}"
        cardtype="${text('cardtype', 'reale')}"
        faded="${boolean('faded', false)}"
        isbn="${text('isbn', '97-88-80-8620-710')}"
        titolo="${text('titolo', 'title')}"
      />`}
  </Story>
</Preview>

  <Props of="z-card-footer" />

