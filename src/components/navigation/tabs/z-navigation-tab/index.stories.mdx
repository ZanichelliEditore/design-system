import {html} from "lit-html";
import {Meta, Story, Canvas, ArgsTable} from "@storybook/addon-docs";
import {NavigationTabsSize, NavigationTabsOrientation} from "../../../../beans/index";
import {ICONS} from "../../../icons/icons";
import "./index";

# ZNavigationTab

<Meta
  title="ZNavigationTab"
  component="z-navigation-tab"
  argTypes={{
    selected: {
      control: "boolean",
    },
    disabled: {
      control: "boolean",
    },
    size: {
      control: {type: "inline-radio"},
      options: Object.values(NavigationTabsSize),
    },
    orientation: {
      control: {type: "inline-radio"},
      options: Object.values(NavigationTabsOrientation),
    },
    htmlTitle: {
      control: {type: "text"},
    },
    icon: {
      control: {type: "select"},
      options: Object.keys(ICONS).sort(),
    },
    label: {
      control: {type: "text"},
    },
  }}
/>

<Canvas>
  <Story
    name="z-navigation-tab"
    args={{
      selected: false,
      disabled: false,
      size: NavigationTabsSize.BIG,
      orientation: NavigationTabsOrientation.HORIZONTAL,
      htmlTitle: "Download stuff",
      icon: "download",
      label: "Download",
    }}
  >
    {(args) => html`
      <z-navigation-tab
        .selected=${args.selected}
        .disabled=${args.disabled}
        .size=${args.size}
        .orientation=${args.orientation}
        .htmlTitle=${args.htmlTitle}
        .icon=${args.icon}
        .label=${args.label}
      ></z-navigation-tab>
    `}
  </Story>
</Canvas>

<ArgsTable of="z-navigation-tab" />
