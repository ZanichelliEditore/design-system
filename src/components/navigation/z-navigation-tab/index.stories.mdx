import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { text, number, select, boolean } from '@storybook/addon-knobs';

## Stories

<Meta title="Navigation/ZNavigationTab" component="z-navigation-tab" />

# ZNavigationTab

<Preview>
  <Story name="z-navigation-tab">
    {(args) => html`
      <z-navigation-tab>
        <z-icon name="${text("icon", "app-switcher")}" slot="icon"></z-icon>
        <span slot="label">${text("label", "App")}</span>
      </z-navigation-tab>`}
  </Story>
</Preview>

<Props of="z-navigation-tab" />
