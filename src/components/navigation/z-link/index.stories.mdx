import {html} from "lit-html";
import {Meta, Story, Canvas, ArgsTable} from "@storybook/addon-docs";
import "./index";

# ZLink

<Meta
  title="ZLink"
  component="z-link"
  argTypes={{
    textcolor: {
      control: {type: "select"},
      options: ["primary", "inverse", "black", "white"],
    },
    iconposition: {
      control: {type: "select"},
      options: ["left", "right"],
    },
  }}
  args={{
    href: "http://www.zanichelli.it",
    target: "_blank",
    textcolor: "primary",
    icon: "download",
    iconposition: "left",
    htmlid: 1,
    htmltabindex: 1,
    isdisabled: false,
    isactive: false,
    underline: false,
  }}
/>

## How to use

Icon dimension is calculated using the link font size multiplied by 1.125. To change the font size set the --font-size-link variable in style; defaults to inherit (es. inline).

Current available text color is : primary, inverse, black and white. Primary and Inverse use the theming values from design-tokens; Black and White has fixed colors.

## Primary

<Canvas>
  <Story name="Primary">
    {(args) => html`<z-link
      href="${args.href}"
      target="${args.target}"
      textcolor="${args.textcolor}"
      icon="${args.icon}"
      iconposition="${args.iconposition}"
      htmlid="${args.htmlid}"
      htmltabindex="${args.htmltabindex}"
      isdisabled="${args.isdisabled}"
      isactive="${args.isactive}"
      underline="${args.underline}"
      >z-link</z-link
    >`}
  </Story>
</Canvas>

## Inverse

<Canvas>
  <Story name="Inverse">
    {(args) => html`<span style="display: inline-flex; padding: var(--space-unit); background: var(--gray900);">
      <z-link
        href="${args.href}"
        target="${args.target}"
        textcolor="inverse"
        icon="${args.icon}"
        iconposition="${args.iconposition}"
        htmlid="${args.htmlid}"
        htmltabindex="${args.htmltabindex}"
        isdisabled="${args.isdisabled}"
        isactive="${args.isactive}"
        underline="${args.underline}"
        >z-link</z-link
      >
    </span>`}
  </Story>
</Canvas>

## Inline without icon

<Canvas>
  <Story name="Inline without icon">
    {(args) => html`<span class="body-1"
      >Clicca sullo
      <z-link
        href="${args.href}"
        target="${args.target}"
        textcolor="${args.textcolor}"
        htmlid="${args.htmlid}"
        htmltabindex="${args.htmltabindex}"
        isdisabled="${args.isdisabled}"
        isactive="${args.isactive}"
        underline="true"
        >z-link</z-link
      >.</span
    >`}
  </Story>
</Canvas>

<ArgsTable of="z-link" />
