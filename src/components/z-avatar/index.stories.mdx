import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { text, select } from "@storybook/addon-knobs";

<Meta title="ZAvatar" component="z-avatar" />

# ZAvatar

<Preview>
  <Story name="ZAvatar-sample">
    {html`<z-avatar
      size="${select("size", ["small", "medium", "large"], "medium")}"
      text="${text("text", "Avatar")}"
      text-color="${text("text-color", "text-white")}"
      background-color="${text("background-color", "bg-grey-700")}"
      image="${text(
        "image",
        "https://cdn.eso.org/images/thumb700x/eso1907a.jpg"
      )}"
    ></z-avatar>`}
  </Story>
</Preview>

<Props of="z-avatar" />
