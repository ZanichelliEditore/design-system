import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs";
import { text, radios, boolean } from '@storybook/addon-knobs';
import { TooltipPosition } from "../../../beans/index.tsx";
import "./index.stories.css";
import "./index";

## Stories

<Meta title="Notification/ZTooltip" component="z-tooltip" />

# ZTooltip

<Preview>
  <Story name="buttons">
    {(args) => html`
      <div class="tooltip-container">
        <z-tooltip
          type="${radios('type', TooltipPosition, 'top')}"
          open="${boolean('open', true)}"
          bind-to="#icon"
        >
          <div class="container">
            <z-button>Open</z-button>
            <z-button>Save as</z-button>
            <z-button>Share</z-button>
          </div>
        </z-tooltip>
        <z-icon id="icon" name="plus-square-filled"></z-icon>
      </div>
    `}
  </Story>
</Preview>

<Preview>
  <Story name="Bound to an element">
    {() => html`
      <div class="tooltip-container">
        <z-tooltip class="dark"
          bind-to="#icon"
          type="${radios('type', TooltipPosition, 'bottom')}"
          open="${boolean('open', true)}"
        >
          <span>Open</span>
        </z-tooltip>
        <z-icon id="icon" name="informationsource"></z-icon>
      </div>
    `}
  </Story>
</Preview>

<Preview>
  <Story name="legacy">
    {(args) => html`
      <z-tooltip
        content="${text('content', 'Aggiungi libro')}"
        type="${radios('type', TooltipPosition, 'top')}"
      ></z-tooltip>`}
  </Story>
</Preview>

<Preview>
  <Story name="legacy dark mode">
    {html`
      <z-tooltip
        class="dark"
        content="${text('content', 'Aggiungi libro')}"
        type="${radios('type', TooltipPosition, 'top')}"
      ></z-tooltip>
    `}
  </Story>
</Preview>

<Props of="z-tooltip" />
