import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { text, select, radios } from '@storybook/addon-knobs';
import { TooltipPosition } from "../../../beans/index.tsx";
import "./index.stories.css";

## Stories

<Meta title="Notification/ZTooltip" component="z-tooltip" />

# ZTooltip

<Preview>
  <Story name="buttons">
    {(args) => html`
      <z-tooltip type="${select('type', TooltipPosition, 'top')}">
        <div class="container">
          <z-button>Open</z-button>
          <z-button>Save as</z-button>
          <z-button>Share</z-button>
        </div>
      </z-tooltip>
    `}
  </Story>
</Preview>

<Preview>
  <Story name="legacy">
    {(args) => html`
      <z-tooltip
        content="${text('content', 'Aggiungi libro')}"
        type="${select('type', TooltipPosition, 'top')}"
      ></z-tooltip>`}
  </Story>
</Preview>

<Preview>
  <Story name="legacy dark mode">
    {html`
      <z-tooltip
        class="dark"
        content="${text('content', 'Aggiungi libro')}"
        type="${select('type', TooltipPosition, 'top')}"
      ></z-tooltip>
    `}
  </Story>
</Preview>

<Preview>
  <Story name="With an element">
    {() => html`
      <div class="tooltip-container">
        <z-tooltip class="dark" type="${radios('type', TooltipPosition, 'bottom')}">
          <span>Open</span>
        </z-tooltip>
        <button>Element</button>
      </div>
    `}
  </Story>
</Preview>

<Props of="z-tooltip" />
