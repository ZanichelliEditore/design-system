import { html } from "lit-html";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs";
import { text, boolean, select } from "@storybook/addon-knobs";
import "./index";

<Meta
  title="Notification/ZToastNotification"
  component="z-toast-notification"
/>

# ZToastNotification

<Preview>
  <Story name="ZToastNotification-slideInRight">
    {html` <style>
        .toast-container {
          z-index: 10000;
          position: fixed;
          top: 0;
          left: 0;
          text-align: center;
          background: aliceblue;
          margin: 40px;
        }
      </style>
      <div class="toast-container">
        <z-toast-notification
          heading="${text("heading", "Titolo")}"
          message="${text("message", "Questo è un messaggio di prova.")}"
          closebutton="${boolean("closebutton", true)}"
          autoclose="${text("autoclose", "")}"
          pauseonfocusloss="${boolean("pauseonfocusloss", false)}"
          type="${select(
            "type",
            ["dark", "light", "accent", "error", "success", "warning"],
            "dark"
          )}"
          isdraggable="${boolean("isdraggable", true)}"
          draggablepercentage="${text("draggablepercentage", "80")}"
          transition="${select(
            "transition",
            ["slide-in-left", "slide-in-right", "slide-in-down", "slide-in-up"],
            "slide-in-right"
          )}"
        ></z-toast-notification>
      </div>`}
  </Story>
  <Story name="ZToastNotification-slideInLeft">
    {html` <style>
        .toast-container {
          z-index: 10000;
          position: fixed;
          top: 0;
          right: 0;
          text-align: center;
          background: aliceblue;
          margin: 40px;
        }
      </style>
      <div class="toast-container">
        <z-toast-notification
          heading="${text("heading", "Titolo")}"
          message="${text("message", "Questo è un messaggio di prova.")}"
          closebutton="${boolean("closebutton", true)}"
          autoclose="${text("autoclose", "")}"
          pauseonfocusloss="${boolean("pauseonfocusloss", false)}"
          type="${select(
            "type",
            ["dark", "light", "accent", "error", "success", "warning"],
            "dark"
          )}"
          isdraggable="${boolean("isdraggable", true)}"
          draggablepercentage="${text("draggablepercentage", "80")}"
          transition="${select(
            "transition",
            ["slide-in-left", "slide-in-right", "slide-in-down", "slide-in-up"],
            "slide-in-left"
          )}"
        ></z-toast-notification>
      </div>`}
  </Story>
  <Story name="ZToastNotification-slideInDown">
    {html` <style>
        .toast-container {
          z-index: 10000;
          position: fixed;
          top: 0;
          left: 0;
          text-align: center;
          background: aliceblue;
          margin: 40px;
        }
      </style>
      <div class="toast-container">
        <z-toast-notification
          heading="${text("heading", "Titolo")}"
          message="${text("message", "Questo è un messaggio di prova.")}"
          closebutton="${boolean("closebutton", true)}"
          autoclose="${text("autoclose", "")}"
          pauseonfocusloss="${boolean("pauseonfocusloss", false)}"
          type="${select(
            "type",
            ["dark", "light", "accent", "error", "success", "warning"],
            "dark"
          )}"
          isdraggable="${boolean("isdraggable", true)}"
          draggablepercentage="${text("draggablepercentage", "80")}"
          transition="${select(
            "transition",
            ["slide-in-left", "slide-in-right", "slide-in-down", "slide-in-up"],
            "slide-in-down"
          )}"
        ></z-toast-notification>
      </div>`}
  </Story>
  <Story name="ZToastNotification-slideInUp">
    {html` <style>
        .toast-container {
          z-index: 10000;
          position: fixed;
          bottom: 0;
          left: 0;
          text-align: center;
          background: aliceblue;
          margin: 40px;
        }
      </style>
      <div class="toast-container">
        <z-toast-notification
          heading="${text("heading", "Titolo")}"
          message="${text("message", "Questo è un messaggio di prova.")}"
          closebutton="${boolean("closebutton", true)}"
          autoclose="${text("autoclose", "")}"
          pauseonfocusloss="${boolean("pauseonfocusloss", false)}"
          type="${select(
            "type",
            ["dark", "light", "accent", "error", "success", "warning"],
            "dark"
          )}"
          isdraggable="${boolean("isdraggable", true)}"
          draggablepercentage="${text("draggablepercentage", "80")}"
          transition="${select(
            "transition",
            ["slide-in-left", "slide-in-right", "slide-in-down", "slide-in-up"],
            "slide-in-up"
          )}"
        ></z-toast-notification>
      </div>`}
  </Story>
  <Story name="ZToastNotification-slottedButton">
    {html` <style>
        .toast-container {
          z-index: 10000;
          position: fixed;
          top: 0;
          left: 0;
          text-align: center;
          background: aliceblue;
          margin: 40px;
        }
      </style>
      <div class="toast-container">
        <z-toast-notification
          heading="${text("heading", "Titolo")}"
          message="${text("message", "Questo è un messaggio di prova.")}"
          closebutton="${boolean("closebutton", true)}"
          autoclose="${text("autoclose", "")}"
          pauseonfocusloss="${boolean("pauseonfocusloss", false)}"
          type="${select(
            "type",
            ["dark", "light", "accent", "error", "success", "warning"],
            "dark"
          )}"
          isdraggable="${boolean("isdraggable", true)}"
          draggablepercentage="${text("draggablepercentage", "80")}"
          transition="${select(
            "transition",
            ["slide-in-left", "slide-in-right", "slide-in-down", "slide-in-up"],
            "slide-in-down"
          )}"
        >
          <z-button-deprecated slot="button" variant="dark-bg" size="small"
            >button</z-button-deprecated
          >
        </z-toast-notification>
      </div>`}
  </Story>
</Preview>

<Props of="z-toast-notification" />
