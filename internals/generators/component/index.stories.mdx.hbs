import { Meta, Canvas, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-html";
import "./index";

<Meta title="{{ properCase name }}" component="{{ kebabCase name }}" />

# {{ properCase name }}

<Canvas>
  <Story name="{{ properCase name }}-sample" args={/* args object */}>
    {(args) => html`<{{kebabCase name}}></{{kebabCase name}}>`}
  </Story>
</Canvas>

<ArgsTable of="{{ kebabCase name }}" />
